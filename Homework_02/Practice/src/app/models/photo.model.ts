export class Photo {
  car_url: string;
  logo_url: string;
  founder_url: string;

  constructor(car_url: string, logo_url: string, founder_url: string){
    this.car_url = car_url;
    this.logo_url = logo_url;
    this.founder_url = founder_url;
  }
}